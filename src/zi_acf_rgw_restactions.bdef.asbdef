managed with additional save implementation in class ZBP_I_ACF_RGW_REST_ACTIONS unique;
strict;
with draft;
define behavior for ZI_ACF_RGW_RestActions alias RESTActions
draft table ZACF_RGWCFR_D_S
with unmanaged save
lock master total etag LastChangedAtMax
authorization master( global )

{
  field ( readonly )
   SingletonID;

  field ( features : instance )
   TransportRequestID;

  field ( notrigger )
   SingletonID,
   LastChangedAtMax;


  update;
  internal create;
  internal delete;

  draft action ( features : instance ) Edit with additional implementation;
  draft action Activate optimized;
  draft action Discard;
  draft action Resume;
  draft determine action Prepare {
    validation RESTAction ~ ValidateDataConsistency;
  }
  action ( features : instance ) SelectCustomizingTransptReq parameter D_SelectCustomizingTransptReqP result [1] $self;

  association _RESTAction { create ( features : instance ); with draft; }

  validation ValidateTransportRequest on save ##NOT_ASSIGNED_TO_DETACT { create; update; }

  side effects {
    action SelectCustomizingTransptReq affects $self;
  }
  }

define behavior for ZI_ACF_RGW_RestActionsTP alias RESTAction ##UNMAPPED_FIELD
persistent table ZACF_RGWCFR
draft table ZACF_RGWCFR_D
etag master LocalLastChangedAt
lock dependent by _RESTActions
authorization dependent by _RESTActions

{
  field ( mandatory : create )
   App,
   ActionKey;

  field ( readonly )
   SingletonID,
   CreatedBy,
   CreatedAt,
   LastChangedBy,
   LastChangedAt,
   LocalLastChangedAt;

  field ( readonly : update )
   App,
   ActionKey;

  field ( notrigger )
   SingletonID,
   CreatedBy,
   CreatedAt,
   LastChangedBy,
   LastChangedAt,
   LocalLastChangedAt;


  update( features : global );
  delete( features : global );

  factory action ( features : instance ) CopyRESTAction parameter ZD_ACF_RGW_CopyAction [1];

  mapping for ZACF_RGWCFR
  {
    App = APP;
    ActionKey = ACTION_KEY;
    IsActive = IS_ACTIVE;
    Description = DESCRIPTION;
    Version = VERSION;
    Tags = TAGS;
    Responsible = RESPONSIBLE;
    ReferenceInfo = REFERENCE_INFO;
    CcCategory = CC_CATEGORY;
    ActionCategory = ACTION_CATEGORY;
    ActionType = ACTION_TYPE;
    ActionHandler = ACTION_HANDLER;
    ActionUrl = ACTION_URL;
    ActionDestination = ACTION_DESTINATION;
    ActionForwardApp = ACTION_FORWARD_APP;
    ActionForwardKey = ACTION_FORWARD_KEY;
    ActionConfig = ACTION_CONFIG;
    RequestContentType = REQUEST_CONTENT_TYPE;
    RequestTemplateDdic = REQUEST_TEMPLATE_DDIC;
    RequestTemplateRaw = REQUEST_TEMPLATE_RAW;
    RequestTemplateJson = REQUEST_TEMPLATE_JSON;
    ResponseContentType = RESPONSE_CONTENT_TYPE;
    ResponseTemplateDdic = RESPONSE_TEMPLATE_DDIC;
    ResponseTemplateRaw = RESPONSE_TEMPLATE_RAW;
    ErrorDefaultStatus = ERROR_DEFAULT_STATUS;
    ErrorDefaultMessage = ERROR_DEFAULT_MESSAGE;
    ErrorDefaultReason = ERROR_DEFAULT_REASON;
    ParameterInfo = PARAMETER_INFO;
    Parameter1 = PARAMETER_1;
    Parameter2 = PARAMETER_2;
    Parameter3 = PARAMETER_3;
    Parameter4 = PARAMETER_4;
    Parameter5 = PARAMETER_5;
    CreatedBy = CREATED_BY;
    CreatedAt = CREATED_AT;
    LastChangedBy = LAST_CHANGED_BY;
    LastChangedAt = LAST_CHANGED_AT;
    LocalLastChangedAt = LOCAL_LAST_CHANGED_AT;
  }

  association _RESTActions { with draft; }

  validation ValidateTransportRequest on save ##NOT_ASSIGNED_TO_DETACT { create; update; delete; }
  validation ValidateDataConsistency on save { create; update; }
}